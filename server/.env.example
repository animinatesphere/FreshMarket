DATABASE_URL="postgresql://freshmarket:freshmarket@localhost:5432/freshmarket?schema=public"
JWT_SECRET="change-this-to-a-long-random-string"
PORT=4000

# Comma-separated list of frontend origins allowed to call this API (no spaces needed, trimmed automatically)
CLIENT_URL="http://localhost:5173,https://your-app.vercel.app"

# Public base URL of THIS backend, used to build image URLs returned by /api/upload.
# Set this to your deployed backend URL (e.g. https://your-api.onrender.com) in production.
PUBLIC_URL="http://localhost:4000"
